<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Maintenance - {{ metadata.siteName }}</title>
        <meta name="description" content="Site en maintenance">
        <link rel="apple-touch-icon" sizes="180x180" href="{{ metadata.siteUrl }}{{ metadata.faviconUrl }}apple-touch-icon.png">
        <link rel="icon" type="image/png" sizes="32x32" href="{{ metadata.siteUrl }}{{ metadata.faviconUrl }}favicon-32x32.png">
        <link rel="icon" type="image/png" sizes="16x16" href="{{ metadata.siteUrl }}{{ metadata.faviconUrl }}favicon-16x16.png">
        <link rel="manifest" href="{{ metadata.siteUrl }}{{ metadata.faviconUrl }}site.webmanifest">
        <link rel="mask-icon" href="{{ metadata.siteUrl }}{{ metadata.faviconUrl }}safari-pinned-tab.svg" color="#332f35">
        <meta name="copyright" content="{{ metadata.siteName }}">
        <meta name="author" content="{{ metadata.siteName }}">
        <style>
            :root {
                --fond: #332F35;
                --texte: #FDFDFE;
                --hover: #54DD93;
                --ascii1: #ed5565;
                --ascii2: #ffce54;
                --ascii3: #97c671;
                --ascii4: #ffffff;
                --ascii5: #a4e9e9;
                --ascii6: #4fc1e9;
            }
            [data-mode="light"] {
                --fond: #FDFDFE;
                --texte: #575757;
                --hover: #00873E;
                --ascii1: #636363;
                --ascii2: #5A5A5A;
                --ascii3: #5E5E5E;
                --ascii4: #070707;
                --ascii5: #686868;
                --ascii6: #676767;
            }
            html {
            }
            body {
                display: flex;
                justify-content: center;
                align-items: center;
                min-height: 100vh;
                margin: 0;
                background: var(--fond);
                color: var(--texte);
                font-family: sans-serif;
            }
            .center {
                text-align: center;
            }
            .hide {
                display: none;
            }
            .fill--mode {
                fill: var(--texte);
            }

            .fill--mode:hover {
                fill: var(--hover);
            }
            .hide-checkbox {
                position: absolute;
                top: -9999rem;
                left: -9999rem
            }
            .sr-only {
                position: absolute;
                width: 1px;
                height: 1px;
                padding: 0;
                overflow: hidden;
                clip: rect(0,0,0,0);
                white-space: nowrap;
                border: 0;
            }
            label:hover {
                cursor: pointer;
            }
            input:focus + label svg {
                outline: .2rem dashed var(--hover);
                outline-offset: .2rem;
            }
            .ascii {
                font-family: monospace;
                font-size: 11px;
                line-height: normal;
            }
            .ascii__couleur-1 { color: var(--ascii1)}
            .ascii__couleur-2 { color: var(--ascii2)}
            .ascii__couleur-3 { color: var(--ascii3)}
            .ascii__couleur-4 { color: var(--ascii4)}
            .ascii__couleur-5 { color: var(--ascii5)}
            .ascii__couleur-6 { color: var(--ascii6)}
        </style>
    </head>
    <body>
        <div class="center">
            {% set logoAsciiSansLien %}true{% endset %}
            {% include "include/logo-ascii.njk" %}
            <h1>Site en maintenance</h1>
            {% include "include/switch-mode.html" %}
        </div>
        <script>
            {% include "include/dark-light-mode.js" %}
        </script>
        {% include "include/svg.html" %}
    </body>
</html>
