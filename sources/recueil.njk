---
layout: layout/standard.njk
titre: "Recueil"
tags: "menuPrincipal"
titreMenu: "Recueil"
exclureRss: "true"
pasDeTitre: "true"
description: "Journal de bord, tutoriel et écrit sur mon site personnel, mes créations et réflexions"
---

{% for article in collections.myPosts %}
    <h2>{{ article.data.title }}</h2>
{% endfor %}

<ul class="ul-reset">
    {% for blog in collections.recueil | reverse %}

        {% if blog.data.brouillon %}
        {% else %}
            <li class="mb-m">

            {% if blog.data.dossierRecueil %}
                {% set classEspacement = " d-block mb-s" %}
            {% endif %}

                <a href="{{ blog.url | url }}" class="tg1-1 td-0 tw-4{{ classEspacement }}">

                    <span class="ts-l th-09">
                        {{ blog.data.titre | safe }}
                    </span>
                    <span class="ts-s tg1-2">—&nbsp;{{ blog.date | frDate | safe }}</span>

                </a>


                {% if blog.data.dossierRecueil == "noteAccessibilite" %}
                    <ul class="pl-s">
                        {% for dossier in collections.noteAccessibilite | reverse %}
                            {% if dossier.data.brouillon %}
                            {% else %}
                                <li>
                                    <a href="{{ dossier.url | url }}" class="tg1-1 td-0 tw-4">
                                        {{ dossier.data.titre | safe }}
                                        <span class="ts-s tg1-2">—&nbsp;{{ blog.date | frDate | safe }}</span>
                                    </a>
                                </li>
                            {% endif %}
                        {% endfor %}
                    </ul>
                {% endif %}


            </li>
        {% endif %}

    {% endfor %}
</ul>
