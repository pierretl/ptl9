<!DOCTYPE html>
<html lang="fr">
    <head>
        {% include "../include/head.njk" %}
        {% if styleCss %}
            <link href="/js/{{ styleCss }}" rel="stylesheet"/>
        {% endif %}
    </head>
    <body class="d-flex fd-c">
        {% include "../include/evitement.html" %}
        <header class="d-flex jc-sb ai-c fw-w pt-m px-tranche bordure-b">
            <div class="mb-m">
                {% include "../include/titre-page.njk" %}
            </div>
            <nav class="d-flex mb-s">
                <span style="width:{{ metadata.tailleLogo }}" class="mr-xs"></span>
                <ul class="d-flex ai-c mr-m__enfant mb-s__enfant">
                    {% for menu in collections.menuPrincipal %}
                        <li class="{% if page.url == menu.url %} tg1-1 td-1 tw-7{% endif %}">
                            {% if page.url == menu.url %}
                                {% if menu.data.titreMenu %}
                                    {{ menu.data.titreMenu }}
                                {% else %}
                                    {{ menu.data.titre }}
                                {% endif %}
                            {% else %}
                                <a class="tg1-1 td-0 tw-7" href="{{ menu.url | url }}">
                                    {% if menu.data.titreMenu %}
                                        {{ menu.data.titreMenu }}
                                    {% else %}
                                        {{ menu.data.titre }}
                                    {% endif %}
                                </a>
                            {% endif %}
                        </li>
                    {% endfor %}
                    <li>
                        <a class="tg1-1 td-0 tw-7" href="mailto:&#99;on&#116;&#97;%63&#116;&#64;%70i&#101;%72%72e%2e&#116;%6c">
                            Contact
                        </a>
                    </li>
                    <li>
                        {% include "../include/switch-mode.html" %}
                    </li>
                </ul>
            </nav>
        </header>
        <main id="contenu" tabindex="-1" role="main" class="s_page{% if markdown %} markdown{% endif %}{% if accueil %} var--mosaique{% endif %}{% if classCssMain %} {{ classCssMain }}{% endif %}">

            {% include "../include/fil-ariane.njk" %}
            
            {% if wip %}
                <p class="ts-xl tg1-4 mb-l">
                    (En cours de rédaction / mise en forme)
                </p>
            {% endif %}
            
            {% if accueil or pasDeTitre %}
            {% else %}
                <h1 class="ts-xl tg1-1 tw-4 mb-m">{{titre}}</h1>
            {% endif %}

            {{content | safe}}
        </main>
        {% include "../include/footer.html" %}
        <script>
            {% include "include/dark-light-mode.js" %}
        </script>
        {% if scriptJs %}
            <script src="/js/{{ scriptJs }}"></script>
        {% endif %}
        {% include "../include/svg.html" %}
    </body>
</html>
