# .mise/config.toml
# Configuration des outils pour Netlify, format TOML valide

[tools]
python = "3.10.12"  # Python < 3.11 pour éviter l'erreur 'rU' avec node-gyp
node = "20.x"       # Même version que le Node local
